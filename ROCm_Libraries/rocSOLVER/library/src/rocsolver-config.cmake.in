
@PACKAGE_INIT@
    
set_and_check(rocsolver_INCLUDE_DIR @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(rocsolver_INCLUDE_DIRS @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(ROCSOLVER_INCLUDE_DIR @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(ROCSOLVER_INCLUDE_DIRS @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(rocsolver_INCLUDE_DIR @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(rocsolver_INCLUDE_DIRS @PACKAGE_INCLUDE_INSTALL_DIR@)

set_and_check(_TARGET_FILE @PACKAGE_CONFIG_PACKAGE_INSTALL_DIR@/rocsolver-targets.cmake)

find_dependency(hip)

include(${_TARGET_FILE})

set(rocsolver_LIBRARIES rocsolver-targets)

set(rocsolver_LIBRARY rocsolver-targets)

set(ROCSOLVER_LIBRARIES rocsolver-targets)

set(ROCSOLVER_LIBRARY rocsolver-targets)

set(rocsolver_LIBRARIES rocsolver-targets)

set(rocsolver_LIBRARY rocsolver-targets)
